# Database Configuration (PostgreSQL / Neon Serverless)
# Direct connection (Migrations / Local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/expense_tracker?schema=public"

# Neon Serverless Pooled connection (Recommended for Production / Vercel with PgBouncer)
# DATABASE_URL="postgresql://user:password@ep-cool-project-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&pgbouncer=true&connect_timeout=15"

# Authentication Secret (Used to sign JWT session cookies)
AUTH_SECRET="your-super-secret-random-jwt-key-change-this-in-production-min-32-chars"
